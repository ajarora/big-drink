<div class="hero-unit">
  <h1>the big drink</h1>
  <p>
    Welcome <%= @user.name if @user %> to the big drink! A map of drinks will go in this area soon.
  </p>

	<% if @user %>

	<% else %>
		<%= link_to(Koala::Facebook::OAuth.new.url_for_oauth_code(:permissions => "publish_stream",
															  :callback => oauth_redirect_url),
					class: "fb_button fb_button_medium") do %>
						Login
		<% end %>
	<% end %>
</div>

<%= will_paginate @drinks %>

<ul class="thumbnails">
  	<%= render @drinks %>
</ul>

<%= will_paginate @drinks %>